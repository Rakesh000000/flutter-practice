import 'package:flutter/material.dart';

class CustomChatHead extends StatelessWidget {
  const CustomChatHead({super.key, required this.title});
  final String title;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(8.0),
      child: Row(
        children: [
          
        ],
      ),
    );
  }
}